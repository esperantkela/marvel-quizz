import React from 'react'
import Levels from '../Levels'
import ProgressBar from '../ProgressBar'

const Quiz = ({ userData }) => {
  
  return (
    <div>
      <Levels/>
      <ProgressBar/>
    </div>
  )
}

export default Quiz